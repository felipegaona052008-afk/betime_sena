Options -Indexes
RewriteEngine On

# Si el archivo o carpeta existe físicamente, sírvelo directamente (css, js, imgs)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Todo lo demás va a index.php
RewriteRule ^ index.php [QSA,L]
